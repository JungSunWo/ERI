<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ERI.demo.mappers.AnonymousUserMapper">

    <!-- 익명 사용자 등록 -->
    <insert id="insertAnonymousUser" parameterType="com.ERI.demo.vo.AnonymousUserVO" useGeneratedKeys="true" keyProperty="anonymousId">
        INSERT INTO TB_ANONYMOUS_USER (
            NICKNAME,
            REG_DATE,
            USE_YN,
            DEL_YN
        ) VALUES (
            #{nickname},
            CURRENT_TIMESTAMP,
            'Y',
            'N'
        )
    </insert>

    <!-- 익명 사용자 조회 -->
    <select id="selectAnonymousUser" resultType="com.ERI.demo.vo.AnonymousUserVO">
        SELECT 
            ANONYMOUS_ID,
            NICKNAME,
            REG_DATE,
            USE_YN,
            DEL_YN,
            DEL_DATE
        FROM TB_ANONYMOUS_USER
        WHERE ANONYMOUS_ID = #{anonymousId}
          AND DEL_YN = 'N'
    </select>

    <!-- 닉네임으로 익명 사용자 조회 -->
    <select id="selectAnonymousUserByNickname" resultType="com.ERI.demo.vo.AnonymousUserVO">
        SELECT 
            ANONYMOUS_ID,
            NICKNAME,
            REG_DATE,
            USE_YN,
            DEL_YN,
            DEL_DATE
        FROM TB_ANONYMOUS_USER
        WHERE NICKNAME = #{nickname}
          AND DEL_YN = 'N'
    </select>

    <!-- 익명 사용자 목록 조회 -->
    <select id="selectAnonymousUserList" resultType="com.ERI.demo.vo.AnonymousUserVO">
        SELECT 
            ANONYMOUS_ID,
            NICKNAME,
            REG_DATE,
            USE_YN,
            DEL_YN,
            DEL_DATE
        FROM TB_ANONYMOUS_USER
        WHERE DEL_YN = 'N'
        ORDER BY REG_DATE DESC
    </select>

    <!-- 익명 사용자 삭제 (논리 삭제) -->
    <update id="deleteAnonymousUser">
        UPDATE TB_ANONYMOUS_USER
        SET DEL_YN = 'Y',
            DEL_DATE = CURRENT_TIMESTAMP
        WHERE ANONYMOUS_ID = #{anonymousId}
    </update>

</mapper> 