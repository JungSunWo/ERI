<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ERI.demo.mappers.PgmAprvDtlMapper">

    <select id="selectPgmAprvDtlList" parameterType="com.ERI.demo.vo.PgmAprvDtlVO" resultType="com.ERI.demo.vo.PgmAprvDtlVO">
        SELECT
            emp_id,
            pgm_id,
            trns_dt,
            seq,
            aprv_sts_cd,
            aprv_rsn,
            rgtr_id,
            reg_dt,
            mdfr_id,
            mdfcn_dt
        FROM
            tb_pgm_aprv_dtl
        <where>
            <if test="empId != null and empId != ''">
                AND emp_id = #{empId}
            </if>
            <if test="pgmId != null and pgmId != ''">
                AND pgm_id = #{pgmId}
            </if>
            <if test="trnsDt != null">
                AND trns_dt = #{trnsDt}
            </if>
            <if test="seq != null">
                AND seq = #{seq}
            </if>
            <if test="aprvStsCd != null and aprvStsCd != ''">
                AND aprv_sts_cd = #{aprvStsCd}
            </if>
        </where>
    </select>

    <select id="selectPgmAprvDtl" resultType="com.ERI.demo.vo.PgmAprvDtlVO">
        SELECT
            emp_id,
            pgm_id,
            trns_dt,
            seq,
            aprv_sts_cd,
            aprv_rsn,
            rgtr_id,
            reg_dt,
            mdfr_id,
            mdfcn_dt
        FROM
            tb_pgm_aprv_dtl
        WHERE
            emp_id = #{empId} AND pgm_id = #{pgmId} AND trns_dt = #{trnsDt} AND seq = #{seq}
    </select>

    <insert id="insertPgmAprvDtl" parameterType="com.ERI.demo.vo.PgmAprvDtlVO">
        INSERT INTO tb_pgm_aprv_dtl (
            emp_id,
            pgm_id,
            trns_dt,
            seq,
            aprv_sts_cd,
            aprv_rsn,
            rgtr_id,
            reg_dt,
            mdfr_id,
            mdfcn_dt
        ) VALUES (
            #{empId},
            #{pgmId},
            #{trnsDt},
            #{seq},
            #{aprvStsCd},
            #{aprvRsn},
            #{rgtrId},
            NOW(),
            #{mdfrId},
            NOW()
        )
    </insert>

    <update id="updatePgmAprvDtl" parameterType="com.ERI.demo.vo.PgmAprvDtlVO">
        UPDATE
            tb_pgm_aprv_dtl
        <set>
            <if test="aprvStsCd != null and aprvStsCd != ''">
                aprv_sts_cd = #{aprvStsCd},
            </if>
            <if test="aprvRsn != null and aprvRsn != ''">
                aprv_rsn = #{aprvRsn},
            </if>
            <if test="mdfrId != null and mdfrId != ''">
                mdfr_id = #{mdfrId},
            </if>
            mdfcn_dt = NOW()
        </set>
        WHERE
            emp_id = #{empId} AND pgm_id = #{pgmId} AND trns_dt = #{trnsDt} AND seq = #{seq}
    </update>

    <delete id="deletePgmAprvDtl">
        DELETE FROM tb_pgm_aprv_dtl
        WHERE emp_id = #{empId} AND pgm_id = #{pgmId} AND trns_dt = #{trnsDt} AND seq = #{seq}
    </delete>

</mapper> 